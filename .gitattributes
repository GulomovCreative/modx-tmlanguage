# Единый вид переводов строк в репозитории. Без этого git на Windows выдаёт
# рабочую копию с CRLF, и снапшоты — файлы, которые сравниваются побайтово —
# перестают совпадать с тем, что генерируется.
* text=auto eol=lf

# Двоичное, трогать нельзя.
*.wasm binary
