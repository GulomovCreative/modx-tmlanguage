# One form of line ending throughout the repository. Without this git on
# Windows hands out a working copy with CRLF, and the snapshots -- files that
# are compared byte for byte -- stop matching what is generated.
* text=auto eol=lf

# Binary: leave it alone.
*.wasm binary
